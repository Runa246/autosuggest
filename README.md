# autosuggest
Verifying my git set-up
This is to check second commit

changes flow for pushing:
1.add(plus button)
2.commit with message
3.push /sync

hi

Commands:
git add -A
git commit -m "hello"
git push

hello